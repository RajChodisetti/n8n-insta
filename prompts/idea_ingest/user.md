Abstract idea:
{{abstract_idea}}

Allowed confidence labels:
{{allowed_confidence_labels_json}}

Duration bounds:
- minimum: {{target_duration_min_seconds}} seconds
- default: {{target_duration_default_seconds}} seconds
- maximum: {{target_duration_max_seconds}} seconds

Return a single JSON object with title, category, confidence_label, target_duration_seconds, summary, notes, source_urls, context, and creative_defaults.
