# Prompt Contract Examples

These examples are sanitized contract examples for future prompt/schema sessions. They are not provider outputs and are not loaded by the current runtime.

Validate parseability with:

```bash
find prompts/examples -name '*.json' -print0 | xargs -0 -n1 jq empty
```
