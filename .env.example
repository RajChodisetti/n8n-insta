# Copy to the repo-root `.env` and fill with real values locally.
# Never commit real API tokens.
INSTAGRAM_GRAPH_API_TOKEN=
GRAPH_API_VERSION=v25.0

# Explicit safety switch for live Instagram publishing from n8n.
INSTAGRAM_PUBLISH_ENABLED=false

# Optional public JPEG URL used by `wf_simple_post_image_asset`.
# If unset, the workflow falls back to the repo-local SVG smoke-test asset,
# which is not publishable through the Instagram API.
MVP_SIMPLE_POST_IMAGE_URL=
